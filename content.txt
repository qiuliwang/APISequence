class Unzip { unzip(String zipFile,String outputPath){ if(outputPath == null) outputPath = ""; outputPath+=File.separator; File outputDirectory = File(outputPath); if(outputDirectory.exists()) outputDirectory.delete(); outputDirectory.mkdir(); { ZipInputStream zip = ZipInputStream(new FileInputStream(zipFile)); ZipEntry entry = null; len; byte[] buffer = byte[1024]; while((entry = zip.getNextEntry()) != null){ if(!entry.isDirectory()){ System.out.println("-"+entry.getName()); File file = File(outputPath +entry.getName()); if(!new File(file.getParent()).exists()) File(file.getParent()).mkdirs(); FileOutputStream fos = FileOutputStream(file); ((len = zip.read(buffer)) > 0) { fos.write(buffer, 0, len); } fos.close(); } } }catch (FileNotFoundException e) { e.printStackTrace(); } (IOException e) { e.printStackTrace(); } } } 
